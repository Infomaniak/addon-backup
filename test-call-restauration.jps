type: update
name: Add-on restauration

targetNodes:

  nodeGroup: '*'

onInstall:

  - cmd[${settings.node}]: |-

      curl -fsS https://raw.githubusercontent.com/axelJacquet/addon-backup/master/restauration.sh --output restauration.sh

      chmod +x restauration.sh

      ./restauration.sh -i "${settings.id}" -d ${settings.destination}

    user: root

responses:
  4109:
    type: error
    message: Les IDS renseignés ne sont pas bons, veuillez vérifiez dans /tmp/Backup_plan


success:

  text: Le backup correspondant à l'id ${settings.id} à était restauré avec succé dans ${settings.destination}

  mail: Le backup correspondant à l'id ${settings.id} à était restauré avec succé dans ${settings.destination}
